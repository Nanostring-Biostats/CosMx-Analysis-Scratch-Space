% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clusterwise_foldchange_metrics_protein.R
\name{clusterwise_foldchange_metrics_protein}
\alias{clusterwise_foldchange_metrics_protein}
\title{Compute fold-change, proportion of positive cells, average expression metrics for each protein in each cluster
(comparing with all other clusters)}
\usage{
clusterwise_foldchange_metrics_protein(
  raw = NULL,
  normed = NULL,
  totalexpr = NULL,
  metadata,
  cluster_column,
  propd = NULL
)
}
\arguments{
\item{raw}{proteins x cells expression matrix.  this only needs to be provided if \code{normed} is not specified.
When \code{normed} is not specified, \code{raw} will be 'totalcount-normalized' before
foldchange metrics are calculated.}

\item{normed}{proteins x cells matrix of data to be used for calculating fold change statistics by clusters.
If this is not provided, \code{raw} will be 'totalexpr-normalized'.
If you desire to use raw expression for computing fold change, use like \code{normed=my_raw_expr}.}

\item{totalexpr}{optional user-specified cell-length vector of 'totalexpression' used for normalizing the raw matrix.
(useful if a subsetted raw matrix is passed)}

\item{metadata}{metadata including (at minimum) 'cellid_column' and a 'cluster_column'}

\item{cluster_column}{column in metadata corresponding to cell type}

\item{propd}{an expression matrix used for computing the proportion of 'positive cells' for a protein.
For example, given a pearson-residual normalized expression matrix (\code{pearson_norm_mat}), we could pass
\code{propd = (pearson_norm_mat > 0)}, or to use a stricter threshold,  \code{propd = (pearson_norm_mat > 1)}}

\item{cellid_column}{column in metadata corresponding to cell id. Should also correspond to the column names of the \code{raw} and \code{normed} expression matrices.}
}
\description{
This function computes metrics by protein and by cluster which can be passed as input to the \code{marker_heatmap()} function.
}
