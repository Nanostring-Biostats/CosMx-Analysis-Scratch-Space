% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_metagene_scores.R
\name{fit_metagene_scores}
\alias{fit_metagene_scores}
\title{Construct celltype metagene scores for clustering using \code{make_markerslist} object via non-negative least squares.}
\usage{
fit_metagene_scores(
  markerslist,
  counts_matrix,
  adjacency_matrix = NULL,
  prior_level_weights = NULL,
  training_ids = NULL,
  modeltype = c("nnls"),
  lag_response = TRUE,
  lag_predictors = TRUE,
  pearson.response = TRUE,
  pearson.scale.max = 10,
  pearson.family = c("poisson", "nonzero_bernoulli"),
  totalcounts = NULL,
  gene_wise_frequency = NULL,
  normalize_predictors_by_totalcounts = TRUE,
  row_standardize_adjacency_matrix = TRUE,
  obs = NULL,
  batch_variable = NULL,
  cellid_colname = "cell_ID",
  ...
)
}
\arguments{
\item{markerslist}{a markerslist created using the \code{make_markerslist} function}

\item{counts_matrix}{a cells x genes expression matrix.
If modeling scaled pearson residuals (pearson.response = TRUE  by default)
,this should be a raw counts matrix.}

\item{adjacency_matrix}{an optional cells x cells matrix matrix of weights denoting similarity between pairs of cells.
For example, one could use the graph of smoothed nearest neighbors distances used to optimize UMAP embeddings.}

\item{prior_level_weights}{an optional vector of weights (typically in range of 0-1), denoting confidence that the cells belong to any of the classes in provided markerslist.
For example, if I am constructing metagene scores for different types of 'tcells',
the weights may be posterior probabilities that the cells are 'tcells', which are generated in a previous modeling stage.}

\item{training_ids}{optional vector of cell IDs to use for training NNLS metagene models.}

\item{modeltype}{Right now, just 'nnls' (non-negative least squares) is supported.}

\item{lag_response}{Should the (weighted) average of the index gene across similar cells be included as a predictor in the regression model? default is TRUE}

\item{lag_predictors}{Should the (weighted) averages of predictor genes across similar cells be included as predictors in the regression model? default is TRUE}

\item{pearson.response}{Should the response variable be the pearson residuals of the index variable?}

\item{pearson.scale.max}{If \code{pearson.response = TRUE}, have large pearson residuals > \code{pearson.scale.max} SDs above the mean truncated to this amount.}

\item{pearson.family}{"poisson" pearson residuals (recommended), or "nonzero_bernoulli" (experimental).}

\item{totalcounts}{If passing in a subset of all genes in the \code{counts_matrix}, this can be a precomputed vector of 'totalcounts' for each cell, which would be used in calculation of pearson residuals and totalcount normalization.}

\item{gene_wise_frequency}{If passing in a subset of all genes in the \code{counts_matrix}, this can be precomputed gene-wise frequencies used to construct pearson residuals.
See \code{gene_frequency()} for examples in generating this.}

\item{normalize_predictors_by_totalcounts}{default is TRUE.}

\item{row_standardize_adjacency_matrix}{default is TRUE; row-standardize the adjacency matrix , such that the sum of neighbor weights across rows adds up to 1.}

\item{obs}{Optional data.frame or data.table of metadata with a column for \code{batch_variable} and the \verb{cell id}.  If provided and \code{pearson.response=TRUE} this is used to construct the pearson residuals}

\item{batch_variable}{Optional column name of 'batch_variable' in \code{obs} dataframe. If provided and \code{pearson.response=TRUE} this is used to construct the pearson residuals}

\item{cellid_colname}{column name of 'cell ids' in the \code{obs} dataframe.}
}
\value{
a 'markerslist' list object which can be passed to \code{fit_metagene_scores}
}
\description{
Construct celltype metagene scores for clustering using \code{make_markerslist} object via non-negative least squares.
}
