% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_pipeline.R
\name{make_pipeline}
\alias{make_pipeline}
\title{Create a 'pipeline' object that can be used to chain together hierarchical cell typing steps.}
\usage{
make_pipeline(markerslists, priors = NULL, priors_category = NULL)
}
\arguments{
\item{markerslists}{named list of markerslists objects (created using \code{make_markerslist()} function),
for each celltyping stage.}

\item{priors}{if a celltyping stage 'A' depends on another celltyping stage 'B', then specify it as a prior,
i.e., \code{priors = list("A" = "B")}}

\item{priors_category}{if a celltyping stage 'A' depends on another celltyping stage 'B', then specify the parent category in 'B' that 'A' is inheriting from ,
i.e., \code{priors_category = list("A" = "B_type")}}
}
\description{
Create a 'pipeline' object that can be used to chain together hierarchical cell typing steps.
}
\examples{

pipeline_tcell <- 
make_pipeline(markerslists = list("tmajor" = smiSmooth::markerslist_tcellmajor
                                  ,"t4minor" = smiSmooth::markerslist_cd4tminor
                                  ,"t8minor" = smiSmooth::markerslist_cd8tminor)
              ,priors = list("t4minor" = "tmajor"
                             ,"t8minor" = "tmajor")
              ,priors_category = list("t4minor" = "cd4t"
                                      ,"t8minor" = "cd8t")
)


}
