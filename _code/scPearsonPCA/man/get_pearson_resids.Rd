% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_pearson_resids.R
\name{get_pearson_resids}
\alias{get_pearson_resids}
\title{Compute the full dense quasi-poisson Pearson residuals matrix}
\usage{
get_pearson_resids(
  x,
  totalcounts = NULL,
  grate = NULL,
  quasi_poisson_variance_inflation = 1.01
)
}
\arguments{
\item{x}{A sparse genes x cells counts matrix, or a plain numeric vector of counts for a single gene (in which case \code{totalcounts} must be provided)}

\item{totalcounts}{The total UMI counts across all gene targets in each cell.  This can be pre-computed by the user and passed in cases where the counts matrix \code{x} contains only a
subset (of, say, highly variable) genes to be used in PCA.}

\item{grate}{The gene frequencies.  This can be pre-computed by the user and passed in cases where the counts matrix \code{x} contains only a
subset (of, say, highly variable) genes to be used in PCA.}

\item{quasi_poisson_variance_inflation}{factor of overdispersion for Variance relative to the mean.  i.e., V(x) = E(x) * phi, where phi is the variance inflation factor}
}
\value{
A dense genes x cells matrix of Pearson residuals.
}
\description{
Directly computes the (dense) genes x cells Pearson residuals matrix for counts matrix \code{x},
without the genes x genes algebra trick used by \code{sparse_quasipoisson_pca_seurat()}.
Intended for validation on small matrices, since the resulting dense matrix can overwhelm
memory for large datasets.
}
