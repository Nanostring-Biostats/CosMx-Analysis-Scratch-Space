% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{chunked_weighted_crossprod}
\alias{chunked_weighted_crossprod}
\title{Chunked weighted cross-product for semi-dense sparse matrices}
\usage{
chunked_weighted_crossprod(X, y_r, w_r, chunk_size = 10000L)
}
\arguments{
\item{X}{sparse N x p dgCMatrix (Xsigned assembled on the R side)}

\item{y_r}{dense N x m response matrix (double)}

\item{w_r}{prior_level_weights vector of length N, or length-0 numeric(0) for unweighted}

\item{chunk_size}{number of rows per BLAS chunk (default 10000)}
}
\value{
named list: a = p x p (X'WX), b = p x m (X'Wy)
}
\description{
Processes rows of X in dense chunks so each t(X)*X call hits BLAS dsyrk/dgemm,
avoiding CHOLMOD's sparse path that is slow at >10\% density.
Peak memory is O(chunk_size * p) instead of O(N * p).
}
